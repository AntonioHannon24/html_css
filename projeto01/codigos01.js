let time = 2000,
    currentTextIndex = 0,
    text = document
                .querySelectorAll("#slider h2"),                    
    max = text.length;

function start() {
    setInterval(() => {
        // troca de image
       
        
    }, time)
}
window.addEventListener("load", start)